spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/clientes_pedidos
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
